"use strict";
document.addEventListener("DOMContentLoaded", ()=> {
   
});
$(function(){
    console.log("jQuery jq version",jQuery.fn.jquery);

        
        function liveSearch(valeur) {
           
           // window.location = Baseurl;
         console.log(Baseurl)
        }
       
        $(".langTr").each((i, val) => {
            //  console.log( JSON.parse("{{ json_encode($photo) }}"));
            ;
          //  $(val).on('mouseover', function () {
                
                // console.log($(this))
                // console.log( "http://localhost/velo_lara/public/" +"en")
                // window.location =  "http://localhost/velo_lara/public/" +"en";
              //  liveSearch($(val).text());
          //  });
        });
       
        
 
    
  });

{
 }
